{% schema %}
{
  "name": "Hero Slider",
  "class": "section-hero-slider",
  "tag": "section",
  "settings": [
    {
      "type": "range",
      "id": "autoplay_speed",
      "min": 3,
      "max": 10,
      "step": 1,
      "unit": "s",
      "label": "Autoplay speed",
      "default": 5
    },
    {
      "type": "checkbox",
      "id": "autoplay",
      "label": "Enable autoplay",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_arrows",
      "label": "Show navigation arrows",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_dots",
      "label": "Show pagination dots",
      "default": true
    },
    {
      "type": "select",
      "id": "height",
      "label": "Slider height",
      "options": [
        { "value": "h-[400px] md:h-[500px]", "label": "Medium" },
        { "value": "h-[500px] md:h-[600px]", "label": "Large" },
        { "value": "h-[600px] md:h-[700px]", "label": "Extra large" },
        { "value": "h-[80vh]", "label": "Full viewport" }
      ],
      "default": "h-[500px] md:h-[600px]"
    }
  ],
  "blocks": [
    {
      "type": "slide",
      "name": "Slide",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Background image"
        },
        {
          "type": "image_picker",
          "id": "mobile_image",
          "label": "Mobile background image (optional)"
        },
        {
          "type": "url",
          "id": "slide_link",
          "label": "Slide link"
        },
        {
          "type": "range",
          "id": "overlay_opacity",
          "min": 0,
          "max": 80,
          "step": 10,
          "unit": "%",
          "label": "Overlay opacity",
          "default": 0
        }
      ]
    },
    {
      "type": "heading",
      "name": "Heading",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Heading text",
          "default": "Bring the spa home"
        },
        {
          "type": "select",
          "id": "color",
          "label": "Text color",
          "options": [
            { "value": "text-black", "label": "Black" },
            { "value": "text-white", "label": "White" }
          ],
          "default": "text-black"
        },
        {
          "type": "range",
          "id": "padding_top",
          "min": 0,
          "max": 100,
          "step": 4,
          "unit": "px",
          "label": "Padding top",
          "default": 0
        },
        {
          "type": "range",
          "id": "padding_bottom",
          "min": 0,
          "max": 100,
          "step": 4,
          "unit": "px",
          "label": "Padding bottom",
          "default": 16
        },
        {
          "type": "range",
          "id": "padding_left",
          "min": 0,
          "max": 100,
          "step": 4,
          "unit": "px",
          "label": "Padding left",
          "default": 24
        },
        {
          "type": "range",
          "id": "padding_right",
          "min": 0,
          "max": 100,
          "step": 4,
          "unit": "px",
          "label": "Padding right",
          "default": 24
        },
        {
          "type": "range",
          "id": "max_width",
          "min": 200,
          "max": 2500,
          "step": 50,
          "unit": "px",
          "label": "Maximum Width",
          "default": 1800
        }
      ]
    },
    {
      "type": "subheading",
      "name": "Subheading",
      "settings": [
        {
          "type": "textarea",
          "id": "text",
          "label": "Subheading text",
          "default": "The five-star Body Wash that transforms your shower."
        },
        {
          "type": "select",
          "id": "color",
          "label": "Text color",
          "options": [
            { "value": "text-black", "label": "Black" },
            { "value": "text-white", "label": "White" }
          ],
          "default": "text-black"
        },
        {
          "type": "range",
          "id": "padding_top",
          "min": 0,
          "max": 100,
          "step": 4,
          "unit": "px",
          "label": "Padding top",
          "default": 0
        },
        {
          "type": "range",
          "id": "padding_bottom",
          "min": 0,
          "max": 100,
          "step": 4,
          "unit": "px",
          "label": "Padding bottom",
          "default": 40
        },
        {
          "type": "range",
          "id": "padding_left",
          "min": 0,
          "max": 100,
          "step": 4,
          "unit": "px",
          "label": "Padding left",
          "default": 24
        },
        {
          "type": "range",
          "id": "padding_right",
          "min": 0,
          "max": 100,
          "step": 4,
          "unit": "px",
          "label": "Padding right",
          "default": 24
        },
        {
          "type": "range",
          "id": "max_width",
          "min": 200,
          "max": 2500,
          "step": 50,
          "unit": "px",
          "label": "Maximum Width",
          "default": 900
        }
      ]
    },
    {
      "type": "button",
      "name": "Button",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Button text",
          "default": "SHOP NOW"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Button link"
        },
        {
          "type": "range",
          "id": "padding_top",
          "min": 0,
          "max": 100,
          "step": 4,
          "unit": "px",
          "label": "Padding top",
          "default": 40
        },
        {
          "type": "range",
          "id": "padding_bottom",
          "min": 0,
          "max": 100,
          "step": 4,
          "unit": "px",
          "label": "Padding bottom",
          "default": 16
        },
        {
          "type": "range",
          "id": "padding_left",
          "min": 0,
          "max": 100,
          "step": 4,
          "unit": "px",
          "label": "Padding left",
          "default": 24
        },
        {
          "type": "range",
          "id": "padding_right",
          "min": 0,
          "max": 100,
          "step": 4,
          "unit": "px",
          "label": "Padding right",
          "default": 24
        },
        {
          "type": "range",
          "id": "max_width",
          "min": 200,
          "max": 2500,
          "step": 50,
          "unit": "px",
          "label": "Maximum Width",
          "default": 500
        }
      ]
    },
    {
      "type": "promo_toast",
      "name": "Promo Toast",
      "limit": 1,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Small product image"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Promo text",
          "default": "Free Mini Santal Deodorant on orders $65+"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Promo link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Hero Slider",
      "blocks": [
        {
          "type": "slide"
        },
        {
          "type": "slide"
        }
      ]
    }
  ]
}
{% endschema %}

{%- assign slide_count_actual = section.blocks | where: 'type', 'slide' | size -%}

<section
  class="hero-slider relative overflow-hidden group {{ section.settings.height }}"
  x-data="{ 
    currentSlide: 0,
    slideCount: {{ section.blocks | where: 'type', 'slide' | size }},
    autoplay: {{ section.settings.autoplay }},
    autoplaySpeed: {{ section.settings.autoplay_speed }},
    interval: null,
    
    init() {
      if (this.autoplay && this.slideCount > 1) {
        this.startAutoplay()
      }
    },
    startAutoplay() {
      this.interval = setInterval(() => { this.next() }, this.autoplaySpeed * 1000)
    },
    pause() { if (this.interval) clearInterval(this.interval) },
    resume() { if (this.autoplay && this.slideCount > 1) this.startAutoplay() },
    next() { this.currentSlide = (this.currentSlide + 1) % this.slideCount },
    prev() { this.currentSlide = (this.currentSlide - 1 + this.slideCount) % this.slideCount },
    goTo(index) { this.currentSlide = index }
  }"
  x-init="init()"
  @mouseenter="pause()"
  @mouseleave="resume()"
>
  <div class="slides-container relative h-full w-full">
    {%- assign slide_counter = 0 -%}
    {%- for block in section.blocks -%}
      {%- if block.type == 'slide' -%}
        {%- assign current_index = slide_counter -%}
        {%- assign slide_counter = slide_counter | plus: 1 -%}
        <div
          class="slide absolute inset-0 h-full w-full transition-all duration-1000 ease-in-out overflow-hidden"
          :class="{ 'opacity-100 z-10': currentSlide === {{ current_index }}, 'opacity-0 z-0': currentSlide !== {{ current_index }} }"
          {{ block.shopify_attributes }}
        >
          {%- if block.settings.slide_link != blank -%}
            <a href="{{ block.settings.slide_link }}" class="absolute inset-0 z-20"></a>
          {%- endif -%}

          {%- if block.settings.image != blank -%}
            <picture class="absolute inset-0 h-full w-full">
              {%- if block.settings.mobile_image != blank -%}
                <source
                  media="(max-width: 768px)"
                  srcset="{{ block.settings.mobile_image | image_url: width: 375 }} 375w,
                          {{ block.settings.mobile_image | image_url: width: 550 }} 550w,
                          {{ block.settings.mobile_image | image_url: width: 768 }} 768w"
                  sizes="100vw">
              {%- endif -%}
              <img
                src="{{ block.settings.image | image_url: width: 1920 }}"
                srcset="{{ block.settings.image | image_url: width: 768 }} 768w,
                        {{ block.settings.image | image_url: width: 1024 }} 1024w,
                        {{ block.settings.image | image_url: width: 1366 }} 1366w,
                        {{ block.settings.image | image_url: width: 1920 }} 1920w"
                sizes="100vw"
                alt="{{ block.settings.image.alt | escape }}"
                class="h-full w-full object-cover"
                loading="{% if forloop.first %}eager{% else %}lazy{% endif %}"
                fetchpriority="{% if forloop.first %}high{% else %}low{% endif %}"
                width="{{ block.settings.image.width }}"
                height="{{ block.settings.image.height }}"
              >
            </picture>
            
            {%- if block.settings.overlay_opacity > 0 -%}
              <div class="absolute inset-0 z-10 bg-black" style="opacity: {{ block.settings.overlay_opacity | divided_by: 100.0 }}"></div>
            {%- endif -%}
          {%- else -%}
            <div class="absolute inset-0 h-full w-full bg-[#f8f8f8]"></div>
          {%- endif -%}
        </div>
      {%- endif -%}
    {%- endfor -%}

<style>
  .hero-content-outer {
    position: absolute;
    inset: 0;
    z-index: 30;
    pointer-events: none;
    display: flex;
    align-items: center;
    padding-left: 10vw !important;
    padding-right: 10vw !important;
  }
  .hero-content-inner {
    width: 100%;
    max-width: 2500px;
    pointer-events: auto;
  }
  .hero-block-style {
    display: block !important;
  }
  .section-hero-slider a.hover\:bg-white:hover,
  .section-hero-slider a:hover.bg-white {
    color: #000 !important;
  }
  @media (max-width: 768px) {
    .hero-content-outer {
      padding-left: 24px !important;
      padding-right: 24px !important;
    }
  }
</style>

    {%- comment -%} Modular Content Overlay (Global) {%- endcomment -%}
    <div class="hero-content-outer">
      <div class="hero-content-inner">
        {%- for block in section.blocks -%}
          {%- case block.type -%}
            {%- when 'heading' -%}
              <h2 
                class="hero-block-style tracking-tighter {{ block.settings.color }}" 
                style="padding: {{ block.settings.padding_top }}px {{ block.settings.padding_right }}px {{ block.settings.padding_bottom }}px {{ block.settings.padding_left }}px; max-width: {{ block.settings.max_width }}px !important; font-size: 60px; line-height: 1.1; font-weight: 400;"
                {{ block.shopify_attributes }}
              >
                {{ block.settings.text }}
              </h2>
            {%- when 'subheading' -%}
              <p 
                class="hero-block-style text-xl md:text-2xl lg:text-[2rem] font-medium opacity-90 {{ block.settings.color }}" 
                style="padding: {{ block.settings.padding_top }}px {{ block.settings.padding_right }}px {{ block.settings.padding_bottom }}px {{ block.settings.padding_left }}px; max-width: {{ block.settings.max_width }}px !important;"
                {{ block.shopify_attributes }}
              >
                {{ block.settings.text }}
              </p>
            {%- when 'button' -%}
              <div 
                class="flex items-center justify-start" 
                style="padding: {{ block.settings.padding_top }}px {{ block.settings.padding_right }}px {{ block.settings.padding_bottom }}px {{ block.settings.padding_left }}px; max-width: {{ block.settings.max_width }}px !important;"
                {{ block.shopify_attributes }}
              >
            <a href="{{ block.settings.link | default: '#' }}" class="inline-flex items-center justify-center gap-8 px-20 py-4 bg-black text-white rounded-full uppercase tracking-[0.15em] text-[15px] transition-all duration-300 hover:bg-white hover:text-black hover:scale-[1.02] group/btn whitespace-nowrap w-fit" style="padding-left: 40px !important; padding-right: 40px !important; font-weight: 500;">
              <span>{{ block.settings.text }}</span>
              <svg width="18" height="12" viewBox="0 0 15 10" fill="none" class="transition-transform duration-300 group-hover/btn:translate-x-1.5 shrink-0" xmlns="http://www.w3.org/2000/svg">
                <path d="M10 1L14 5L10 9" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/>
                <path d="M1 5H14" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </a>
              </div>
          {%- endcase -%}
        {%- endfor -%}
      </div>
    </div>
  </div>
  </div>

  {%- comment -%} Navigation Arrows and Dots removed as per user request {%- endcomment -%}

  {%- comment -%} Promo Toast Component {%- endcomment -%}
  {%- for block in section.blocks -%}
    {%- if block.type == 'promo_toast' -%}
      <div 
        x-data="{ show: true }" 
        x-show="show" 
        x-transition:enter="transition ease-out duration-500"
        x-transition:enter-start="opacity-0 translate-y-4"
        x-transition:enter-end="opacity-100 translate-y-0"
        class="absolute bottom-6 right-8 md:bottom-10 md:right-20 lg:right-32 z-40 bg-[#f8f8f8] p-1.5 pr-4 rounded-2xl flex items-center gap-4 shadow-sm border border-black/5"
        {{ block.shopify_attributes }}
      >
        {%- if block.settings.image != blank -%}
          <div class="h-16 w-16 flex-shrink-0 bg-white rounded-xl overflow-hidden">
            <img 
              src="{{ block.settings.image | image_url: width: 150 }}" 
              alt="{{ block.settings.text }}"
              class="h-full w-full object-contain mix-blend-multiply"
              width="64"
              height="64"
            >
          </div>
        {%- endif -%}
        
        <div class="flex-grow">
          {%- if block.settings.link != blank -%}
            <a href="{{ block.settings.link }}" class="text-[14px] font-bold text-black hover:underline tracking-tight">
              {{ block.settings.text }}
            </a>
          {%- else -%}
             <span class="text-[14px] font-bold text-black tracking-tight">{{ block.settings.text }}</span>
          {%- endif -%}
        </div>

        <button 
          @click="show = false" 
          class="p-2 ml-2 text-black hover:opacity-50 transition-opacity"
          aria-label="Close promo"
        >
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M1 1L13 13" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
            <path d="M13 1L1 13" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
          </svg>
        </button>
      </div>
    {%- endif -%}
  {%- endfor -%}
</section>
