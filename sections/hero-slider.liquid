{% schema %}
{
  "name": "Hero Slider",
  "class": "section-hero-slider",
  "tag": "section",
  "settings": [
    {
      "type": "range",
      "id": "autoplay_speed",
      "min": 3,
      "max": 10,
      "step": 1,
      "unit": "s",
      "label": "Autoplay speed",
      "default": 5
    },
    {
      "type": "checkbox",
      "id": "autoplay",
      "label": "Enable autoplay",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_arrows",
      "label": "Show navigation arrows",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_dots",
      "label": "Show pagination dots",
      "default": true
    },
    {
      "type": "select",
      "id": "height",
      "label": "Slider height",
      "options": [
        { "value": "h-[400px] md:h-[500px]", "label": "Medium" },
        { "value": "h-[500px] md:h-[600px]", "label": "Large" },
        { "value": "h-[600px] md:h-[700px]", "label": "Extra large" },
        { "value": "h-[80vh]", "label": "Full viewport" }
      ],
      "default": "h-[500px] md:h-[600px]"
    }
  ],
  "blocks": [
    {
      "type": "slide",
      "name": "Slide",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Background image"
        },
        {
          "type": "image_picker",
          "id": "mobile_image",
          "label": "Mobile background image (optional)"
        },
        {
          "type": "url",
          "id": "slide_link",
          "label": "Slide link"
        },
        {
          "type": "range",
          "id": "overlay_opacity",
          "min": 0,
          "max": 80,
          "step": 10,
          "unit": "%",
          "label": "Overlay opacity",
          "default": 0
        }
      ]
    },
    {
      "type": "heading",
      "name": "Heading",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Heading text",
          "default": "Bring the spa home"
        },
        {
          "type": "select",
          "id": "color",
          "label": "Text color",
          "options": [
            { "value": "text-black", "label": "Black" },
            { "value": "text-white", "label": "White" }
          ],
          "default": "text-black"
        }
      ]
    },
    {
      "type": "subheading",
      "name": "Subheading",
      "settings": [
        {
          "type": "textarea",
          "id": "text",
          "label": "Subheading text",
          "default": "The five-star Body Wash that transforms your shower."
        },
        {
          "type": "select",
          "id": "color",
          "label": "Text color",
          "options": [
            { "value": "text-black", "label": "Black" },
            { "value": "text-white", "label": "White" }
          ],
          "default": "text-black"
        }
      ]
    },
    {
      "type": "button",
      "name": "Button",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Button text",
          "default": "SHOP NOW"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Button link"
        }
      ]
    },
    {
      "type": "promo_toast",
      "name": "Promo Toast",
      "limit": 1,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Small product image"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Promo text",
          "default": "Free Mini Santal Deodorant on orders $65+"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Promo link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Hero Slider",
      "blocks": [
        {
          "type": "slide"
        },
        {
          "type": "slide"
        }
      ]
    }
  ]
}
{% endschema %}

{%- assign slide_count_actual = section.blocks | where: 'type', 'slide' | size -%}

<section
  class="hero-slider relative overflow-hidden group {{ section.settings.height }}"
  x-data="{ 
    currentSlide: 0,
    slideCount: {{ section.blocks | where: 'type', 'slide' | size }},
    autoplay: {{ section.settings.autoplay }},
    autoplaySpeed: {{ section.settings.autoplay_speed }},
    interval: null,
    
    init() {
      if (this.autoplay && this.slideCount > 1) {
        this.startAutoplay()
      }
    },
    startAutoplay() {
      this.interval = setInterval(() => { this.next() }, this.autoplaySpeed * 1000)
    },
    pause() { if (this.interval) clearInterval(this.interval) },
    resume() { if (this.autoplay && this.slideCount > 1) this.startAutoplay() },
    next() { this.currentSlide = (this.currentSlide + 1) % this.slideCount },
    prev() { this.currentSlide = (this.currentSlide - 1 + this.slideCount) % this.slideCount },
    goTo(index) { this.currentSlide = index }
  }"
  x-init="init()"
  @mouseenter="pause()"
  @mouseleave="resume()"
>
  <div class="slides-container relative h-full w-full">
    {%- assign slide_counter = 0 -%}
    {%- for block in section.blocks -%}
      {%- if block.type == 'slide' -%}
        {%- assign current_index = slide_counter -%}
        {%- assign slide_counter = slide_counter | plus: 1 -%}
        <div
          class="slide absolute inset-0 h-full w-full transition-all duration-1000 ease-in-out overflow-hidden"
          :class="{ 'opacity-100 z-10': currentSlide === {{ current_index }}, 'opacity-0 z-0': currentSlide !== {{ current_index }} }"
          {{ block.shopify_attributes }}
        >
          {%- if block.settings.slide_link != blank -%}
            <a href="{{ block.settings.slide_link }}" class="absolute inset-0 z-20"></a>
          {%- endif -%}

          {%- if block.settings.image != blank -%}
            <picture class="absolute inset-0 h-full w-full">
              {%- if block.settings.mobile_image != blank -%}
                <source
                  media="(max-width: 768px)"
                  srcset="{{ block.settings.mobile_image | image_url: width: 375 }} 375w,
                          {{ block.settings.mobile_image | image_url: width: 550 }} 550w,
                          {{ block.settings.mobile_image | image_url: width: 768 }} 768w"
                  sizes="100vw">
              {%- endif -%}
              <img
                src="{{ block.settings.image | image_url: width: 1920 }}"
                srcset="{{ block.settings.image | image_url: width: 768 }} 768w,
                        {{ block.settings.image | image_url: width: 1024 }} 1024w,
                        {{ block.settings.image | image_url: width: 1366 }} 1366w,
                        {{ block.settings.image | image_url: width: 1920 }} 1920w"
                sizes="100vw"
                alt="{{ block.settings.image.alt | escape }}"
                class="h-full w-full object-cover"
                loading="{% if forloop.first %}eager{% else %}lazy{% endif %}"
                fetchpriority="{% if forloop.first %}high{% else %}low{% endif %}"
                width="{{ block.settings.image.width }}"
                height="{{ block.settings.image.height }}"
              >
            </picture>
            
            {%- if block.settings.overlay_opacity > 0 -%}
              <div class="absolute inset-0 z-10 bg-black" style="opacity: {{ block.settings.overlay_opacity | divided_by: 100.0 }}"></div>
            {%- endif -%}
          {%- else -%}
            <div class="absolute inset-0 h-full w-full bg-[#f8f8f8]"></div>
          {%- endif -%}
        </div>
      {%- endif -%}
    {%- endfor -%}

    {%- comment -%} Modular Content Overlay (Global) {%- endcomment -%}
    <div class="absolute inset-0 z-30 pointer-events-none flex items-center px-8 md:px-20 lg:px-32">
      <div class="max-w-[450px] md:max-w-[600px] pointer-events-auto">
        {%- for block in section.blocks -%}
          {%- case block.type -%}
            {%- when 'heading' -%}
              <h2 class="text-5xl md:text-6xl lg:text-8xl font-bold leading-[1.05] mb-4 tracking-tighter {{ block.settings.color }}" {{ block.shopify_attributes }}>
                {{ block.settings.text }}
              </h2>
            {%- when 'subheading' -%}
              <p class="text-base md:text-lg lg:text-xl font-medium mb-10 opacity-90 max-w-[450px] {{ block.settings.color }}" {{ block.shopify_attributes }}>
                {{ block.settings.text }}
              </p>
            {%- when 'button' -%}
              <div class="mt-4 mb-4" {{ block.shopify_attributes }}>
                <a href="{{ block.settings.link | default: '#' }}" class="inline-flex items-center justify-center gap-6 px-10 py-4 bg-black text-white rounded-full uppercase tracking-[0.1em] text-[12px] font-bold transition-all duration-300 hover:bg-gray-900 group/btn whitespace-nowrap min-w-max">
                  <span>{{ block.settings.text }}</span>
                  <svg width="18" height="12" viewBox="0 0 18 12" fill="none" class="transition-transform duration-300 group-hover/btn:translate-x-1.5 shrink-0" xmlns="http://www.w3.org/2000/svg">
                    <path d="M11.5 1L16.5 6L11.5 11" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
                    <path d="M1.5 6H16.5" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
                  </svg>
                </a>
              </div>
          {%- endcase -%}
        {%- endfor -%}
      </div>
    </div>
  </div>
  </div>

  {%- if section.settings.show_arrows and section.blocks.size > 1 -%}
    <button
      class="nav-arrow absolute left-4 top-1/2 z-30 flex h-10 w-10 -translate-y-1/2 items-center justify-center rounded-full bg-white/10 text-white backdrop-blur-sm transition-all duration-300 hover:bg-white hover:text-black md:left-10 opacity-0 group-hover:opacity-100"
      @click="prev()"
      aria-label="Previous slide"
    >
      <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 19l-7-7 7-7"></path>
      </svg>
    </button>
    <button
      class="nav-arrow absolute right-4 top-1/2 z-30 flex h-10 w-10 -translate-y-1/2 items-center justify-center rounded-full bg-white/10 text-white backdrop-blur-sm transition-all duration-300 hover:bg-white hover:text-black md:right-10 opacity-0 group-hover:opacity-100"
      @click="next()"
      aria-label="Next slide"
    >
      <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 5l7 7-7 7"></path>
      </svg>
    </button>
  {%- endif -%}

  {%- if section.settings.show_dots and slide_count_actual > 1 -%}
    <div class="dots-container absolute bottom-10 left-1/2 -translate-x-1/2 z-30 flex gap-3">
      {%- assign dot_counter = 0 -%}
      {%- for block in section.blocks -%}
        {%- if block.type == 'slide' -%}
          {%- assign dot_index = dot_counter -%}
          {%- assign dot_counter = dot_counter | plus: 1 -%}
          <button
            class="h-1 w-10 transition-all duration-500 rounded-full"
            :class="currentSlide === {{ dot_index }} ? 'bg-white' : 'bg-white/30'"
            @click="goTo({{ dot_index }})"
            aria-label="Go to slide {{ dot_counter }}"
          ></button>
        {%- endif -%}
      {%- endfor -%}
    </div>
  {%- endif -%}

  {%- comment -%} Promo Toast Component {%- endcomment -%}
  {%- for block in section.blocks -%}
    {%- if block.type == 'promo_toast' -%}
      <div 
        x-data="{ show: true }" 
        x-show="show" 
        x-transition:enter="transition ease-out duration-500"
        x-transition:enter-start="opacity-0 translate-y-4"
        x-transition:enter-end="opacity-100 translate-y-0"
        class="absolute bottom-6 right-8 md:bottom-10 md:right-20 lg:right-32 z-40 bg-[#f8f8f8] p-1.5 pr-4 rounded-2xl flex items-center gap-4 shadow-sm border border-black/5"
        {{ block.shopify_attributes }}
      >
        {%- if block.settings.image != blank -%}
          <div class="h-16 w-16 flex-shrink-0 bg-white rounded-xl overflow-hidden">
            <img 
              src="{{ block.settings.image | image_url: width: 150 }}" 
              alt="{{ block.settings.text }}"
              class="h-full w-full object-contain mix-blend-multiply"
              width="64"
              height="64"
            >
          </div>
        {%- endif -%}
        
        <div class="flex-grow">
          {%- if block.settings.link != blank -%}
            <a href="{{ block.settings.link }}" class="text-[14px] font-bold text-black hover:underline tracking-tight">
              {{ block.settings.text }}
            </a>
          {%- else -%}
             <span class="text-[14px] font-bold text-black tracking-tight">{{ block.settings.text }}</span>
          {%- endif -%}
        </div>

        <button 
          @click="show = false" 
          class="p-2 ml-2 text-black hover:opacity-50 transition-opacity"
          aria-label="Close promo"
        >
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M1 1L13 13" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
            <path d="M13 1L1 13" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
          </svg>
        </button>
      </div>
    {%- endif -%}
  {%- endfor -%}
</section>
